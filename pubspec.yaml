name: dashen_bank_app
description: A Dashen Bank mobile application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  #local_auth: ^2.1.6
  local_auth: ^2.3.0  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  

flutter:
  uses-material-design: true
  # No assets section needed - app works with placeholders
  assets:
    - assets/logo.png